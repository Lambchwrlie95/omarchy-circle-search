# omarchy-circle-search — Workflow

## Entry — Hyprland Keybind

```
SUPER + SHIFT + PRINT
        │
        ▼
  ┌─────────────────────┐
  │  omarchy-circle-search  │
  └─────────┬───────────┘
            │
            ▼
    ┌───────────────┐
    │  Parse CLI     │    --copy   → clipboard mode
    │  (clap)        │    --notify → send notification
    └───────┬───────┘
            │
            ▼
    ┌───────────────┐
    │  check_deps()  │    checks for:
    │                │      grim     (screenshot)
    │                │      wl-copy  (clipboard)
    │                │      python3  (upload scripts)
    └───────┬───────┘
            │
     ┌──────┴──────┐
     │              │
     ▼              ▼
  ┌────────┐  ┌──────────┐
  │ theme  │  │  config   │
  │ .rs    │  │  .rs      │
  │        │  │           │
  │ reads  │  │ reads     │
  │ colors │  │ circle-   │
  │ .toml  │  │ search    │
  │        │  │ .toml     │
  │ fall-  │  │ fallback  │
  │ back   │  │ defaults  │
  │ dark   │  │           │
  │ theme  │  │           │
  └────┬───┘  └─────┬─────┘
       │             │
       └──────┬──────┘
              │
              ▼
    ┌──────────────────────┐
    │  spawn_fullscreen_   │
    │  capture()           │ ◄── async: grim runs in
    │  ┌──────────────┐    │     background while GTK
    │  │  grim (full) │    │     initialises
    │  │  → full-*.png│    │
    │  └──────────────┘    │
    └─────────┬────────────┘
              │
              ▼
    ┌──────────────────────┐
    │  gtk4::init()        │
    │  + layer_shell::     │
    │    Overlay           │
    └─────────┬────────────┘
              │
              ▼
    ┌──────────────────────────────┐
    │        OVERLAY               │
    │  ┌────────────────────────┐  │
    │  │ ┌───┐ ┌────┐ ┌────┐   │  │
    │  │ │ 🔍│ │lens│ │yand│ … │  │  ← engine pills
    │  │ └───┘ └────┘ └────┘   │  │
    │  │                        │  │
    │  │  choose engine · draw  │  │  ← instruction
    │  │  to search  · esc      │  │
    │  │                        │  │
    │  │      ╭───╮            │  │  ← Catmull-Rom lasso
    │  │     ╱     ╲           │  │
    │  │    │   ●   │          │  │     start dot
    │  │     ╲     ╱           │  │
    │  │      ╰───╯            │  │
    │  └────────────────────────┘  │
    └─────────┬────────────────────┘
              │
     ┌────────┴────────┐
     │                  │
     ▼                  ▼
  ┌────────┐   ┌──────────────┐
  │  Esc   │   │  Release     │
  │        │   │  drag-end    │
  │ cancel │   └──────┬───────┘
  │ close  │          │
  └────────┘          ▼
              ┌──────────────┐
              │  < 10px ?    │──── shake animation
              │  too small   │     → clear points
              └──────┬───────┘     → retry
                     │ valid
                     ▼
              ┌──────────────┐
              │  calc region  │
              │  from points  │
              │  + 8px pad    │
              └──────┬───────┘
                     │
                     ▼
              ┌──────────────┐
              │  capture_    │
              │  region()    │
              │  ┌────────┐  │
              │  │ grim -g │  │  ← crop region
              │  │ WxH+X+Y │  │
              │  └────────┘  │
              └──────┬───────┘
                     │
                     ▼
              ┌──────────────┐
              │  close GTK   │
              │  window       │
              └──────┬───────┘
                     │
                     ▼
              ┌──────────────┐
              │  --copy?     │──── yes ──► clipboard (wl-copy)
              └──────┬───────┘
                     │ no
                     ▼
              ┌──────────────┐
              │  engine?     │
              └──────┬───────┘
                     │
         ┌───────────┴───────────┐
         │                       │
         ▼                       ▼
  ┌────────────────┐   ┌──────────────────┐
  │  AiChat         │   │  Visual Search   │
  │                 │   │  (lens / yandex  │
  │  ┌───────────┐  │   │   bing / tineye  │
  │  │ wl-copy   │  │   │   saucenao)      │
  │  │ image/png │  │   │                  │
  │  └─────┬─────┘  │   │  ┌────────────┐  │
  │        │         │   │  │ python3 -c │  │
  │  ┌─────┴─────┐  │   │  │ (spawned)  │  │
  │  │ xdg-open  │  │   │  └──────┬─────┘  │
  │  │ chat URL  │  │   │         │         │
  │  └─────┬─────┘  │   │         ▼         │
  │        │         │   │  ┌────────────┐  │
  │  sleep 3s        │   │  │  Imgur     │  │
  │        │         │   │  │  (Client-  │  │
  │  ┌─────┴─────┐  │   │  │   ID auth) │  │
  │  │ wtype     │  │   │  └──────┬─────┘  │
  │  │ Ctrl+V    │  │   │         │         │
  │  │ Enter     │  │   │    ┌────┴────┐    │
  │  └─────┬─────┘  │   │    │ fail?  │    │
  │        │         │   │    └────┬────┘    │
  │  ┌─────┴─────┐  │   │         │ yes     │
  │  │ notify    │  │   │         ▼         │
  │  │ pasted    │  │   │  ┌────────────┐  │
  │  └───────────┘  │   │  │  0x0.st    │  │
  │                 │   │  │  (fallback) │  │
  │                 │   │  └──────┬─────┘  │
  │                 │   │         │         │
  │                 │   │  ┌──────┴──────┐  │
  │                 │   │  │ build_url() │  │
  │                 │   │  │ + xdg-open  │  │
  │                 │   │  └─────────────┘  │
  │                 │   │         │          │
  │                 │   │  ┌──────┴──────┐   │
  │                 │   │  │ notify     │   │
  │                 │   │  │ "Searching" │   │
  │                 │   │  └─────────────┘   │
  └─────────────────┘   └──────────────────┘

## File Map

┌──────────────────────────────────────────────────────────────┐
│  src/main.rs     CLI, dep check, orchestrate flow            │
│  src/overlay.rs  GTK4 layer-shell UI: lasso + engine menu   │
│  src/capture.rs  grim screenshot (fullscreen + region crop)  │
│  src/search.rs   Upload / search / AI chat (python scripts) │
│  src/config.rs   Read ~/.config/omarchy/circle-search.toml  │
│  src/theme.rs    Read ~/.config/omarchy/current/theme/...   │
└──────────────────────────────────────────────────────────────┘

## Key Concepts

  Async capture:     grim spawns BEFORE gtk4::init()
                     PNG loads via 20ms poll timer

  Catmull-Rom:       Smoothed path cached in DrawState.smoothed
                     Re-computed only when a new point added

  Upload chain:      Imgur (Client-ID)
                     → 0x0.st (fallback if Imgur fails)

  Engine keys:       l=Google Lens  y=Yandex  b=Bing
                     c=AI Chat     t=TinEye  s=SauceNao

  Config:            ~/.config/omarchy/circle-search.toml
                     [ai_chat]  url, name, paste_delay
                     [upload]   imgur_client_id
